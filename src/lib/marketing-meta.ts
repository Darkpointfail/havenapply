export const RPA_RESIDENCE_COUNT = 1300;

export function privacyPath(locale: string) {
  return `/${locale}/confidentialite`;
}

export function collectionPath(locale: string) {
  return `/${locale}/avis-de-collecte`;
}

export function termsPath(locale: string) {
  return `/${locale}/conditions`;
}
